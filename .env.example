# Root-level variables consumed by `docker compose` only.
# Application secrets live in per-app .env files (apps/api/.env, apps/web/.env) — never at the root.
# Copy to .env and adjust: `cp .env.example .env`

POSTGRES_USER=monorepo
POSTGRES_PASSWORD=monorepo
POSTGRES_DB=monorepo
POSTGRES_PORT=5432
