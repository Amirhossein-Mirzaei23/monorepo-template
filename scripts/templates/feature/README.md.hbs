# {{name}} feature

Generated by `npm run gen:feature`. Reference pattern: `apps/web/src/features/auth`.

## Structure

- `index.ts` — public barrel (only import path for other modules)
- `components/` — feature-scoped components
- `hooks/` — react-query hooks
- `api/` — fetchers + query keys
- `schemas/` — zod schemas (validation + types)
- `types.ts` — feature-facing type re-exports
- `__tests__/`

## Endpoints touched

| BFF route / URL | Method | Notes |
|---|---|---|
| `/api/{{name}}` | GET | TODO |

## Checklist after scaffolding

- [ ] Point `api/{{name}}-api.ts` at the real endpoint (BFF route or public API)
- [ ] Validate responses with generated schemas from `@monorepo/shared-types`
- [ ] Compose from a route in `src/app`; import only via `@/features/{{name}}`
- [ ] Forms: react-hook-form + `schemas/{{name}}-schema`
