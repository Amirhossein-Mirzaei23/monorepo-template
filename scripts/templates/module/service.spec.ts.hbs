import { {{ pascalCase name }}Service } from '../{{name}}.service';

describe('{{ pascalCase name }}Service', () => {
  it('is defined once wired to its repository', () => {
    // TODO: construct with a FakePrisma-backed repository like users.service.spec.ts
    expect({{ pascalCase name }}Service).toBeDefined();
  });
});
