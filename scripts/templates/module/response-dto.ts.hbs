import { ApiProperty } from '@nestjs/swagger';

/** Public representation of a {{name}} resource. */
export class {{ pascalCase name }}ResponseDto {
  @ApiProperty({ example: 'clx…cuid' })
  id!: string;

  @ApiProperty({ example: '{{ pascalCase name }} title' })
  title!: string;

  @ApiProperty({ example: '2026-01-01T00:00:00.000Z' })
  createdAt!: Date;
}

// TODO: map entities to the response DTO (see modules/users/dto/user-response.dto.ts).
