# {{ pascalCase name }} module

Generated by `npm run gen:module`. Reference pattern: `apps/api/src/modules/users`.

## Endpoints

| Method | Path | Auth | Notes |
|---|---|---|---|
| GET | `/{{name}}` | bearer | paginated list |
| GET | `/{{name}}/:id` | bearer | |
| POST | `/{{name}}` | bearer | |
| PATCH | `/{{name}}/:id` | bearer | |
| DELETE | `/{{name}}/:id` | bearer | |

## Checklist after scaffolding

- [ ] Register `{{ pascalCase name }}Module` in `src/app.module.ts`
- [ ] Add the Prisma model + `prisma migrate dev`
- [ ] Implement repository (data access only) and service (business rules)
- [ ] Add `@Roles(...)` where needed
- [ ] Run `npm run gen:types` if public DTOs changed
- [ ] Tests: unit for the service, e2e for the controller
